// Deprecated: the global subject/level filter was removed when the
// `currentField` context was retired. Page-local filters live next to
// each page now. This module is intentionally left empty.
export {};
