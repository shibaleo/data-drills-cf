# 本番ビルド (vite build = production mode) 専用の上書き。commit 対象・非機密。
# dev では読まれず .env.development の pk_test が使われる。Vite が build mode で自動選択。
# ここに置けるのは VITE_ 接頭辞の公開値のみ。実 secret は bws (詳細は SECRETS.md)。

VITE_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsuc2hpYmFsZW8udWsk
