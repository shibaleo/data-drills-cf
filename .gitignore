# dependencies
node_modules/

# build output
dist/
.wrangler/

# Lambda build artifacts (services/pdf-lambda/build.mjs outputs)
services/pdf-lambda/lambda.zip
services/pdf-lambda/assets/

# env — 方針 (2026-07-22): 非機密 config は src/lib/public-config.ts (git 追跡) に集約。
# secret は注入 (bws / CF Worker Secret)。よって .env* は一切 commit しない (全 ignore)。
# .env.local には bootstrap の BWS_ACCESS_TOKEN のみ。
.env
.env.*
.dev.vars

# secrets
.mcp.json
*_accessKeys.csv
*accessKeys*.csv

# misc
.DS_Store
*.tsbuildinfo
*.pem
*.log
.claude/
out.json
