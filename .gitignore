# dependencies
node_modules/

# build output
dist/
.wrangler/

# Lambda build artifacts (services/pdf-lambda/build.mjs outputs)
services/pdf-lambda/lambda.zip
services/pdf-lambda/assets/

# env — 非機密の共有 config (.env / .env.development / .env.production) は commit する。
# 実 secret は bws。bootstrap token とローカル上書きだけ gitignore。
.env.local
.env.*.local
.dev.vars

# secrets
.mcp.json
*_accessKeys.csv
*accessKeys*.csv

# misc
.DS_Store
*.tsbuildinfo
*.pem
*.log
.claude/
out.json
